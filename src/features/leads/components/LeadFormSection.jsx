import FormSection from "./FormSection";

/** Alias matching the plan component name — wraps FormSection. */
const LeadFormSection = (props) => <FormSection {...props} />;

export default LeadFormSection;
